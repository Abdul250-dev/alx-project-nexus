import { View } from "react-native"
import VideoTutorialScreen from "../../../../screens/Education/VideoTutorialScreen"

export default function VideoScreen() {
  // const { id } = useLocalSearchParams<{ id: string }>()
  return (
    <View style={{ flex: 1 }}>
      <VideoTutorialScreen />
    </View>
  )
}
